{% extends 'base.html.twig' %}

{% block title %}StatusCommande index{% endblock %}

{% block body %}
    <h1>StatusCommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for status_commande in status_commandes %}
            <tr>
                <td>{{ status_commande.id }}</td>
                <td>{{ status_commande.libelle }}</td>
                <td>
                    <a href="{{ path('app_status_commande_show', {'id': status_commande.id}) }}">show</a>
                    <a href="{{ path('app_status_commande_edit', {'id': status_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_status_commande_new') }}">Create new</a>
{% endblock %}
